# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Database Configuration
# For PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://username:password@localhost/lemur_dev

# For SQLite (development/testing):
DATABASE_URL=sqlite:///./lemur.db

# Optional: Enable SQL query logging (true/false)
SQL_ECHO=false

# JWT Secret Key (for authentication - generate a secure random string)
SECRET_KEY=your-secret-key-here-use-openssl-rand-hex-32

# CORS Settings (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# File Upload Settings
MAX_FILE_SIZE_MB=100
ALLOWED_EXTENSIONS=csv,xlsx,xls

# Redis URL (optional, for caching)
# REDIS_URL=redis://localhost:6379/0

# S3/MinIO Configuration (optional, for file storage)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET_NAME=lemur-files
# S3_ENDPOINT_URL=http://localhost:9000  # For MinIO

# Environment (development/staging/production)
ENVIRONMENT=development
EOF < /dev/null